<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Customer address layout style -->
    <template id="address_layout_custom">
        <t t-set="colclass" t-value="('col-sm-5' if report_type == 'html' else 'col-5') + ' me-auto'"/>
        <div t-if="address" class="address row " title="This block is not always present depending on the printed document.">
            <!-- The below default web.address_layout template code is commented out to not show invoice and delivery addresses 
            in Quotation/Sale order report when it is different from customer address -->
            <!-- <t t-if="information_block">
                <t t-set="colclass" t-value="'col-5 offset-2'"/>
                <div name="information_block" class="col-6">
                    <t t-out="information_block or None">
                        <div class="bg-light border-1 rounded h-100 d-flex flex-column align-items-center justify-content-center p-4 opacity-75 text-muted text-center">
                            <strong>Information block</strong>
                            <div>Usually contains a source address or a complementary address.</div>
                        </div>
                    </t>
                </div>
            </t> -->

            <div name="address" t-att-class="colclass">
                <t t-esc="address or None">
                    <div class="bg-light border-1 rounded h-100 d-flex flex-column align-items-center justify-content-center p-4 opacity-75 text-muted text-center">
                        <strong>Address block</strong>
                        <div>Usually contains the address of the document's recipient.</div>
                    </div>
                </t>
            </div>
        </div>
        <div class="oe_structure" t-else=""></div>
    </template>
    
    <!-- Code for template created to configure Custom document layout -->
    <template id="external_layout_custom">
        <!-- Header portion -->
        <div t-attf-class="header o_company_#{company.id}_layout" t-att-style="report_header_style">
            <div class="o_boxed_header">
                <div class="row mb8">
                    <div class="col-8"></div> 
                    <div class="col-4 text-right">
                        <img t-if="company.logo" t-att-src="image_data_uri(company.logo)" alt="Logo" style="width: 100%; max-height: 150px; object-fit: contain;"/>
                    </div>
                </div>
                <div class="row">
                    <div class="col-12 text-end mb4">
                        <t t-if="company.report_header and company.report_header.strip() != '&lt;p&gt;&lt;br&gt;&lt;/p&gt;'">
                            <div class="mt0 h4" t-field="company.report_header"/>
                        </t>
                    </div>
                </div>
            </div>
        </div>

        <!-- Customer address portion -->
        <div t-attf-class="article o_report_layout_boxed o_company_#{company.id}_layout {{  'o_report_layout_background' if company.layout_background in ['Geometric', 'Custom']  else  '' }}" t-attf-style="background-image: url({{ 'data:image/png;base64,%s' % company.layout_background_image.decode('utf-8') if company.layout_background_image and company.layout_background == 'Custom' else '/base/static/img/bg_background_template.jpg' if company.layout_background == 'Geometric' else ''}});" t-att-data-oe-model="o and o._name" t-att-data-oe-id="o and o.id" t-att-data-oe-lang="o and o.env.context.get('lang')">
            <!-- Table layout -->
            <t t-out="0"/>
        </div>

        <!-- Footer portion -->
        <div t-attf-class="footer o_boxed_footer o_company_#{company.id}_layout">
            <div class="text-center">
                <div class="row mt10">
                    <img t-if="company.footer_image" t-att-src="image_data_uri(company.footer_image)" alt="Footer"/>
                </div>
            </div>
        </div>
    </template>
</odoo>